apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  labels:
    app: app-of-apps
  name: app-of-apps-placement
  namespace: helloworld
spec:
  clusterSelector:
    matchLabels:
      'local-cluster': 'true'